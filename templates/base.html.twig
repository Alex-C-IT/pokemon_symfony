<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
        {# Les fichiers se trouvent dans le dossier /css #}
        <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css/bootstrap.rtl.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css/_variables.scss') }}">
        <link rel="stylesheet" href="{{ asset('css/_bootswatch.scss') }}">
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block header %}
            {# Si on est dans la partie admin/ alors charger la navbar admin qui se trouve dans admin/partiels sous le nom '_adminnavbar.html.twig'  #}
            <nav>
                {% if app.request.attributes.get('_route') starts with 'app_admin_' %}
                    {% include 'admin/partials/_adminnavbar.html.twig' %}
                {% else %}
                    {% include 'public/partials/_navbar.html.twig' %}
                {% endif %}
            </nav>
        {% endblock %}
        {% block body %}{% endblock %}
    </body>
</html>
